// Package codeowners holds the format-agnostic CODEOWNERS result types.
// Actual parsing lives in VCS adapters (format varies by platform -
// GitHub's CODEOWNERS, GitLab's CODEOWNERS, Bitbucket's OWNERS, etc.).
package codeowners
